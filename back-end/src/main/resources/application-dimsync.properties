# Profile: dimsync
# Activated by Cloud Run Job via SPRING_PROFILES_ACTIVE=dimsync.
# The job runs DimensionSyncJob then exits; no web server is needed.

spring.main.web-application-type=none
spring.task.scheduling.enabled=false
